{
  "qtype": "mcq",
  "prompt": "Why would a developer use HTML entities for `<` and `>` inside page content?",
  "options": [
    "To make the page load faster",
    "To prevent the browser from interpreting them as tag markers",
    "To enable CSS animations",
    "To add semantic meaning"
  ]
}