# 🏗️ Comments & HTML Entities/Special Characters

## 💬 HTML Comments {#html-comments}

```html
<!-- This is a comment, not shown to the user -->
```

- Not rendered by the browser, but visible in page source — never put sensitive info in comments.
- Cannot be nested.
- Useful for notes to your future self, temporarily disabling code, or marking sections (`<!-- Header -->`, `<!-- End Header -->`).

## ❓ Why Entities Exist {#why-entities-exist}

Certain characters are **<span style="color: #ef4444;">reserved</span>** in HTML because the parser uses them for markup itself — most importantly `<` and `>` (tag delimiters) and `&amp;` (starts an entity/character reference). If you type these literally as content, the browser may misinterpret them as markup. <span style="color: #3b82f6;">Character references</span> let you safely represent them, along with characters not on a standard keyboard (©, emoji-adjacent symbols, non-breaking spaces).

## 🔣 The Anatomy of a Character Reference {#common-entities}

Format: <span style="color: #14b8a6; font-family: monospace;">&amp;name;</span> (named) or <span style="color: #14b8a6; font-family: monospace;">&amp;#number;</span> / <span style="color: #14b8a6; font-family: monospace;">&amp;#xHEX;</span> (numeric), always starting with `&` and ending with `;`.

<div className="overflow-x-auto my-6">
  <table>
    <thead>
      <tr>
        <th>Character</th>
        <th>Entity Name</th>
        <th>Numeric</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>&lt;</code></td>
        <td><code>&amp;lt;</code></td>
        <td><code>&amp;#60;</code></td>
      </tr>
      <tr>
        <td><code>&gt;</code></td>
        <td><code>&amp;gt;</code></td>
        <td><code>&amp;#62;</code></td>
      </tr>
      <tr>
        <td><code>&amp;</code></td>
        <td><code>&amp;amp;</code></td>
        <td><code>&amp;#38;</code></td>
      </tr>
      <tr>
        <td><code>&quot;</code></td>
        <td><code>&amp;quot;</code></td>
        <td><code>&amp;#34;</code></td>
      </tr>
      <tr>
        <td><code>&apos;</code></td>
        <td><code>&amp;apos;</code></td>
        <td><code>&amp;#39;</code></td>
      </tr>
      <tr>
        <td>(non-breaking space)</td>
        <td><code>&amp;nbsp;</code></td>
        <td><code>&amp;#160;</code></td>
      </tr>
      <tr>
        <td>©</td>
        <td><code>&amp;copy;</code></td>
        <td><code>&amp;#169;</code></td>
      </tr>
    </tbody>
  </table>
</div>

## ⚠️ The Three "Always Escape These" Characters {#escaping-reserved-characters}

`<`, `>`, and `&amp;` are the ones that *must* be escaped when they appear as literal content (not markup), since they have structural meaning to the parser.

## 🔠 Case Sensitivity {#whitespace-entities}

Named references are <span style="color: #f59e0b; font-weight: bold;">case-sensitive</span> — `&Amp;` is not the same as `&amp;` and may not resolve correctly.

## ❌ Common Misconceptions {#common-misconceptions}

- Forgetting entities are **<span style="color: #f59e0b;">case-sensitive</span>**.
- Using multiple literal spaces expecting them to render — browsers collapse whitespace to a single space; `&nbsp;` is needed to force extra space (more in Topic 13).
- Assuming comments are hidden from users entirely — they're invisible on the rendered page but fully visible in "View Source."

**Summary in 5 lines:**

- **<span style="color: #10b981;">Comments</span>** (`<!-- -->`) aren't rendered but are visible in source — don't hide secrets there.
- **<span style="color: #ef4444;">Reserved characters</span>** (`<`, `>`, `&amp;`) must be escaped via character references.
- References come in **<span style="color: #3b82f6;">named</span>** (`<`) or **<span style="color: #3b82f6;">numeric</span>** (`&#60;` / `&#x3C;`) form.
- Entity names are **<span style="color: #f59e0b;">case-sensitive</span>**.
- <span style="color: #14b8a6; font-family: monospace;">&amp;nbsp;</span> forces a non-breaking space where normal whitespace would collapse.

---

**<span style="color: #6366f1;">Quick check</span>** — before moving on, answer the checkpoint question below.
