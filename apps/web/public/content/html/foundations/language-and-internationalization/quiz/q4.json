{
  "qtype": "mcq",
  "prompt": "When should you add `lang` and `dir` attributes to an *inner* element (not just `<html>`)?",
  "options": [
    "Never — only the root element needs them",
    "When a section of the page is in a different language or direction",
    "Only for images",
    "Only on form inputs"
  ]
}