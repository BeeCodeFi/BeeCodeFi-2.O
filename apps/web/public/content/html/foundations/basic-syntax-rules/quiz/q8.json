{
  "qtype": "mcq",
  "prompt": "What is the difference between a **tag** and an **element**?",
  "options": [
    "They mean exactly the same thing",
    "Tag = the angle-bracket marker; element = opening tag + content + closing tag",
    "Element = only the content between tags",
    "Tag includes the content; element includes only markers"
  ]
}