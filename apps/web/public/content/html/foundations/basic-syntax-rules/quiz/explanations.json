{
  "q1": "False. <img> is a void element and does not require a closing tag. Void elements like <img>, <br>, <input> have no content and no closing tag.",
  "q2": "disabled. Boolean attributes like disabled, required, and checked don't need a value — their mere presence means true.",
  "q3": "Crossed. The tags are crossed (improperly nested). Proper nesting means closing tags in the reverse order they were opened, like balanced parentheses.",
  "q4": "Element = opening tag + content + closing tag; tag = just the markers. For example, in <p>Hello</p>, the whole thing is an element, while <p> and </p> are tags.",
  "q5": "<br>. Void elements like <br>, <img>, <input>, <hr> have no content and no closing tag.",
  "q6": "The trailing slash is optional and purely stylistic. In HTML, both <br> and <br /> are valid. The trailing slash is optional and only matters in strict XHTML.",
  "q7": "False. Attribute values can use either double or single quotes. Both are valid — the convention is to be consistent, usually with double quotes.",
  "q8": "Browsers may fix it silently but it's invalid and unpredictable. Improper nesting (crossed tags) is invalid HTML, and browsers may attempt to fix it, but the results can be unpredictable.",
  "q9": "<div>. <div> is not a void element — it requires both opening and closing tags. Void elements include <img>, <br>, <input>, <hr>, <meta>, <link>.",
  "q10": "An attribute whose mere presence means true. Boolean attributes like disabled, required, and checked don't need a value — just including them sets them to true."
}