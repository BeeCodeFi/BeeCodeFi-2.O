{
  "q1": "Opening tag, content, and closing tag make up an element — for example: <p>Hello</p>. The opening tag marks the start, the closing tag (with /) marks the end, and the content sits between them.",
  "q2": "<img> is a void element. Void elements like <img>, <br>, <hr>, and <input> can never have children, so they have no closing tag.",
  "q3": "Attributes go inside the opening tag, written as name=\"value\" pairs — for example: <a href=\"https://example.com\">.",
  "q4": "An attribute whose mere presence means true is a boolean attribute. For example, <input disabled> — adding `disabled` enables the feature; omitting it disables it.",
  "q5": "Children must be closed before their parent is closed. Overlapping tags like <b><i></b></i> is invalid; the correct form is <b><i></i></b>.",
  "q6": "Browsers usually recover silently, hiding the bug. HTML parsers are forgiving by design, but this can mask errors that cause layout or accessibility problems.",
  "q7": "Wrapped in double or single quotes. While HTML5 technically allows unquoted values in simple cases, quoting is universally required best practice to avoid parsing errors.",
  "q8": "Tag = the angle-bracket marker; element = opening tag + content + closing tag. <p> is a tag; <p>Hello</p> is an element.",
  "q9": "Zero, one, or many attributes are allowed. An element can have no attributes (<p>), one (<p class=\"note\">), or several (<a href=\"...\" target=\"_blank\" rel=\"noopener\">).",
  "q10": "Optional and purely stylistic. HTML5 accepts both <br> and <br /> — the slash has no functional effect in HTML (unlike in XML/XHTML)."
}