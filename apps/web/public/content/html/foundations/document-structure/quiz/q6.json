{
  "qtype": "mcq",
  "prompt": "What happens when the browser encounters a page with no `<!DOCTYPE html>`?",
  "options": [
    "The page fails to load entirely",
    "The browser switches to quirks mode",
    "CSS is disabled automatically",
    "Nothing noticeable changes"
  ]
}