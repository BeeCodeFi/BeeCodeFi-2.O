{
  "qtype": "mcq",
  "prompt": "Does `<!DOCTYPE html>` require a closing tag?",
  "options": [
    "Yes — `</DOCTYPE>`",
    "Yes — `</html>`",
    "No — it is not an HTML element",
    "Only in XML mode"
  ]
}