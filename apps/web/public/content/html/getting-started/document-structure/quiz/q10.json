{ "qtype": "fix_code", "prompt": "A page has two <body> tags — the first empty, the second holding all the content. What actually happens?", "options": ["The browser refuses to render the page", "The browser throws a fatal parsing error and shows a blank page", "The browser is lenient and typically merges the content into a single rendered body, hiding the mistake", "HTML allows multiple <body> elements by design"] }
