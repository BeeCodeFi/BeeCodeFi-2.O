{ "qtype": "multi", "prompt": "Which of these statements are true? (select all that apply)", "options": ["HTML comments are hidden from anyone viewing page source", "HTML comments can be nested inside each other", "A raw & in text content can be misinterpreted as the start of an entity", "Regular spaces in HTML text collapse to a single space when rendered", "&nbsp; prevents a line break at that position"] }
