{ "qtype": "fix_code", "prompt": "A learner says: 'A URL's href can only ever be a path like /about — it can never include a full domain or scheme.' What's wrong with this claim?", "options": ["Nothing, that's correct", "A URL — and therefore an href — can include a scheme and domain as well as a path; a path alone is only one valid form", "href attributes can only be used inside <img> tags", "Paths are optional but schemes are forbidden in HTML"] }
