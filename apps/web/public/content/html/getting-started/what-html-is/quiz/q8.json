{ "qtype": "fix_code", "prompt": "A learner says: 'The DOM is exactly the same thing as the HTML file I wrote, so it can never be different from my source code.' What's wrong with this statement?", "options": ["Nothing — the DOM and the HTML source are always identical", "The DOM is a live tree the browser builds from the HTML, and JavaScript can change it without touching the original file", "HTML files don't get turned into a DOM at all", "The DOM only exists after the page is closed"] }
