{ "qtype": "multi", "prompt": "Which of these statements about how browsers load a page are true? (select all that apply)", "options": ["The browser parses HTML from top to bottom", "Linked CSS and image files can be requested while the rest of the HTML is still being parsed", "The browser must finish painting the page before it can request any images", "The DOM is built while the browser parses the HTML"] }
