{ "qtype": "mcq", "prompt": "What is the difference between an element and a tag?", "options": ["They are the same thing", "Element = opening tag + content + closing tag; tag = just the markers", "Tag = opening tag + content + closing tag; element = just the markers", "Neither has a specific meaning"] }